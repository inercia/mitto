name: Loop Param Test
description: A loop-selector prompt with a required text param (E2E edit-args dialog testing)
menus: promptsLoop
group: Loop
parameters:
  - name: TASK
    type: text
    multiLine: true
    description: The task to perform on each iteration
    required: false
prompt: |
  Perform the following task on this iteration: ${TASK}.
