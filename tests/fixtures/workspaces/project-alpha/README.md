# Project Alpha

A mock Go project used for testing Mitto.

## Structure

- `main.go` - Entry point
- `src/` - Source files
  - `app.go` - Application logic
  - `utils.go` - Utility functions

## Purpose

This directory serves as a test workspace for Mitto integration and UI tests.
It provides a realistic project structure that the mock ACP server can reference.

