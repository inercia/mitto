# Project Beta

A mock JavaScript/Node.js project used for testing Mitto.

## Structure

- `index.js` - Entry point
- `lib/` - Library files
  - `helpers.js` - Helper functions

## Purpose

This directory serves as a test workspace for Mitto integration and UI tests.
It provides a different project type (Node.js) to test multi-workspace scenarios.

