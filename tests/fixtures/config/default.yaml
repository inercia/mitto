# Default test configuration for Mitto
# Uses mock ACP server for deterministic testing

acp:
  - mock-acp:
      command: ./tests/mocks/acp-server/mock-acp-server --verbose

web:
  host: 127.0.0.1
  port: 8089
  theme: v2

