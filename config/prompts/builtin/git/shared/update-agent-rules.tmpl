{{- /*
  update-agent-rules — offer to persist conventions learned during the session
  into the project's Agent rules/memories. Consumed by git/create-commits,
  git/submit-changes and git/commit-and-submit.

  Heading-less and header-free (single paragraph) — callers place it under
  their own numbered step.
*/ -}}
If during this session you discovered project conventions, patterns, or preferences (or the user corrected assumptions),
update Agent rules/memories. Ask user before modifying rules files.
