{{- /* Standalone PR/MR wrapper around the babysitters' complete collector. */ -}}
For GitHub, use the following shared collection procedure:

{{ template "github/shared/pr-fetch-review-threads" . }}

For GitLab instead:

```bash
glab mr view <number> --comments
```

Supplement with paginated GitLab API MR metadata, discussions and notes to
retain author IDs/usernames, bot flags, note IDs, created_at/updated_at, and
all replies. Build the same FEEDBACK_JSON shape and PR_AUTHOR_LOGIN value
(MR author username); if identity/completeness cannot be established, report
that limitation rather than treating the set as empty or granting authority.

Categorize the merged set into: review comments, inline code comments,
conversation threads, change requests, approvals-with-comments. Preserve
author-authored requests and follow-up replies; distinguish satisfied
discussions from new instructions using the feedback checkpoint below.
