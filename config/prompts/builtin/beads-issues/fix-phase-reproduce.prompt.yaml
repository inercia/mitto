icon: error
name: Bug fix — reproduce phase
menus: internal
parameters:
  - name: IssueID
    type: beadsId
    required: false
    description: The beads issue ID to reproduce
  - name: AdditionalInstructions
    type: text
    required: false
    multiLine: true
    description: Optional additional instructions for this phase, forwarded by the driver that dispatched it.
description: Internal phase-tier prompt — write a failing test that reproduces the bug and add the `reproduced` label. Invoked by name from the bug-fix driver; runs on the Coding tier.
group: Tasks
backgroundColor: '#FFCDD2'
preferredModels:
  - modelTag: Coding
target:
  reuse:
    issue: true
    coalesce: true
prompt: |
  {{ template "_shared/session-context" . }}

  # Bug Fix — Reproduce Phase

  This is the **reproduce** stage of the label-encoded bug-fix state machine
  (`researched` → `reproduced` → `fixed`). It is normally invoked by name from the
  `Loop fixing bug` driver (or by a list orchestrator), which selects this phase
  when the target bead carries `researched` but not yet `reproduced`.

  {{ $target := "" -}}
  {{ if .Session.BeadsIssue }}{{ $target = .Session.BeadsIssue }}{{ else if .Args.IssueID }}{{ $target = .Args.IssueID }}{{ end -}}
  {{ template "beads-issues/shared/target-bead-header-strict" (dict
      "Target"   $target
      "Noun"     "target bug"
      "FromLink" (ne .Session.BeadsIssue "")
  ) }}

  {{ if $target -}}
  {{ template "beads-issues/shared/additional-instructions" . }}

  {{ template "beads-issues/shared/tier-check" (dict
      "Target"    $target
      "Tier"      "Coding"
      "Phase"     "reproduce"
      "Confirmed" (Model "Coding")
      "ModelName" .Session.ModelName
      "ModelTags" .Session.ModelTags
  ) }}

  ## Step 1 — Load LIVE state

  Labels drift between runs. Load the bead's **current** state fresh:

  ```bash
  bd show {{ $target }} --json --include-comments
  ```

  Read its `labels` array and prior comments (especially the investigate-phase
  `Investigation:` comment recorded when `researched` was added). If `reproduced` is
  already present, this phase's work is already done — `bd comment` a note that the
  phase re-ran redundantly and stop **without** re-adding the label.

  ## Step 2 — Write a failing test

  This is a **scheduled, silent** phase (invoked by the driver's on-completion
  loop). Do **not** call `mitto_ui_options`, `mitto_ui_form`, or `mitto_ui_textbox` —
  decide autonomously; if something is genuinely unclear or unsolvable without user
  input, defer via **Step 4**.

  Write an automated test that **fails** because of the bug (no fix yet). Ground the
  test in the investigation notes on the bead:

  - Use the project's existing test framework and conventions (mirror a nearby test).
  - Prefer the **narrowest** level that still demonstrates the bug
    (unit > integration > e2e).
  - Assert the **expected** behavior so the test fails **specifically** on this bug,
    not on an unrelated error or a crash during setup.
  - Name it descriptively; reference the bead ID (`{{ $target }}`) in a comment.

  Run the test and confirm it fails, with the failing output matching the reported
  symptom. Capture the exact command and the failing output as evidence. If it does
  not fail as expected, refine the test — a test that passes has not reproduced the
  bug. **Leave the failing test in place** (do not delete or skip it) so it will
  verify the future fix.

  ## Step 3 — Record and advance the label — MANDATORY CLOSURE

  When the failing test reliably reproduces the bug, execute the following checklist
  **in order**. This is not optional prose: every item must complete before the turn
  ends, and Step 3.V verifies each item actually landed. Historically this closure
  has been the single most common execution miss (agent writes the Reproduction
  comment, then stops without labeling), which leaves the bead lying about the
  state of the tree — the verification step exists specifically to catch that class
  of miss.

  ### Step 3.1 — Record the reproduction on the bead

  ```bash
  bd comment {{ $target }} "Reproduction [tier: Coding]: <failing test file::name, command to run it, failing output>."
  ```

  ### Step 3.2 — Advance the state machine

  Only after Step 3.1 has posted:

  ```bash
  bd update {{ $target }} --add-label reproduced
  ```

  ### Step 3.V — Self-verification (MANDATORY before ending the turn)

  Run each of these commands and confirm the expected result. If any check
  fails, the corresponding step above was NOT completed — go back and finish
  it, then re-run the failing check. Do **not** end the turn with a red check.

  ```bash
  # V.1: bead carries the `reproduced` label
  bd show {{ $target }} --json | python3 -c "import json,sys; d=json.load(sys.stdin); d=d[0] if isinstance(d,list) else d; assert 'reproduced' in d.get('labels',[]), 'MISSING reproduced label'; print('OK: reproduced label present')"

  # V.2: `Reproduction [tier: Coding]:` comment is on the bead
  bd show {{ $target }} --include-comments | grep -F "Reproduction [tier: Coding]:" >/dev/null && echo "OK: Reproduction comment present" || echo "MISSING Reproduction [tier: Coding]: comment"

  # V.3: the failing reproduction test file actually exists on disk
  REPRO_TEST_PATH="<path to the failing test file you just added>"
  test -f "$REPRO_TEST_PATH" && echo "OK: reproduction test file present at $REPRO_TEST_PATH" || echo "MISSING repro test file at $REPRO_TEST_PATH"
  ```

  Only after all checks print `OK`, stop — the driver's next scheduled run will
  observe `reproduced` and dispatch the fix phase.
  {{- end }}

  ## Step 4 — Blocked → Defer + Handoff

  {{ template "beads-issues/shared/blocked-defer-handoff" (dict
      "Target"      $target
      "SessionID"   .Session.ID
      "Placeholder" "<target-bug>"
      "Label"       "reproduced"
      "BlockedAt"   "reproduce"
  ) }}

  ## Guidelines

  - **One stage only.** This phase advances at most one label (`reproduced`).
    Do not attempt to fix the bug — the fix phase runs next.
  - **Test must fail for the right reason.** A test that passes, or fails on an
    unrelated setup error, has not reproduced the bug — refine and retry.
  {{ template "beads-issues/shared/phase-guidelines-footer" (dict
      "IncludeItemNote" false
      "TagNoun"         "summary"
      "TagPrefix"       "Reproduction"
      "Tier"            "Coding"
  ) }}
